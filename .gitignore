# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
venv/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Training artifacts
checkpoints/
runs/
*.ckpt

# Model weights (frozen encoders download at runtime; TRM ships separately)
*.pt
*.pth
*.onnx

# Data
*.npz
data/

# Eval outputs (telemetry JSONL + results JSON from eval/libero_eval.py)
eval_results/

# OS / editor
.DS_Store
.idea/
.vscode/

# Scratch download/extraction dirs (raw shards — must NEVER be committed)
.shard_tmp/
.shard_tmp_*/
*.hdf5
*.tfrecord*
# Runtime logs, checkpoints dir, task outputs
logs/
checkpoints/
*.output
