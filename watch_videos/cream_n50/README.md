# Cream cheese crosses — 18 unaided successes, n=50 held-out

**18/50 = 0.360, Wilson 95% [0.24, 0.50]**, LIBERO-Object task 1, seed 20
(held-out band), `goal_heads_dagger.pt`, `anchor_band` 0.04, no assist flags.
One film per success; all 18 md5-distinct.

Cream cheese scored **0/10 under every previous configuration** in this
campaign — four binders, a prompt-engineering fix, three head versions, and
eight refuted mechanisms. What crossed it was DAgger fine-tuning on the
machine's *own* deployment viewpoints, labelled from the simulator's object
pose (the teacher corpus cannot label viewpoints it never visited).

Collection drew **seed 0**; every film here is from **seed 20**, which the
training data never touched.

Artifact: `eval_results/n50_cream_dagger`. Full record: `paper/paper.md`.
